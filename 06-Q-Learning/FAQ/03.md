Les équations sont correctement formatées. Voici le texte avec les équations en format LaTeX pour un README, utilisant des doubles dollars ($$) :

### 1. Bellman Equation

$$
Q(S_1, \text{right})_{\text{observed}} = R(S_2) + \gamma \max_{a} Q(S_2, a)
$$

### 2. TD Error

$$
\text{TD Error} = Q(S_1, \text{right})_{\text{observed}} - Q(S_1, \text{right})_{\text{expected}}
$$

### 3. Update Rule

$$
Q(S_1, \text{right}) = Q(S_1, \text{right}) + \alpha \times \text{TD Error}
$$

Ces équations sont maintenant correctement formatées pour un README en Markdown avec LaTeX.
